<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- BUG-013 FIX: Updated to Theme.MaterialComponents.DayNight.NoActionBar
         to match values/themes.xml. DayNight handles both light and dark automatically. -->
    <style name="Theme.ClearWalls" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
